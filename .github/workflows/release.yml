name: Release
on:
  workflow_dispatch:
    inputs:
      releaseType:
        description: 'Type of release to create'
        required: true
        type: choice
        options:
          - alpha
          - beta
          - rc
          - stable

concurrency:
  group: ${{ github.workflow }}-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  prepare-release-version:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Get Next Version
        id: semver
        uses: ietf-tools/semver-action@v1.11.0
        with:
          token: ${{ github.token }}
          branch: ${{ github.ref_name }}
          majorList: 'feat!,fix!'
          tagFilter: '^v[0-9]+\.[0-9]+\.[0-9]+$'
          noNewCommitBehavior: 'current'
          noVersionBumpBehavior: 'current'
          skipInvalidTags: true
          maxTagsToFetch: '30'
          scopeList: 'ui,loader,store,network,search,settings,backend,gamemngr'
      - name: Determine release channel
        id: channel
        run: |
          if [ "${{ inputs.releaseType }}" = "stable" ]; then
            echo "isPrerelease=false" >> "$GITHUB_OUTPUT"
            echo "version=${{ steps.semver.outputs.next }}" >> "$GITHUB_OUTPUT"
            echo "versionStrict=${{ steps.semver.outputs.nextStrict }}" >> "$GITHUB_OUTPUT"
          else
            # Per-version sequential: find the highest existing vX.Y.Z-(alpha|beta|rc).N tag and add 1
            lastRelease=$(git tag -l "${{ steps.semver.outputs.next }}-${{ inputs.releaseType }}.*" | sed 's/.*-${{ inputs.releaseType }}\.//' | sort -n | tail -n1)
            releaseNum=$(( ${lastRelease:-0} + 1 ))
            echo "isPrerelease=true" >> "$GITHUB_OUTPUT"
            echo "version=${{ steps.semver.outputs.next }}-${{ inputs.releaseType }}.${releaseNum}" >> "$GITHUB_OUTPUT"
            echo "versionStrict=${{ steps.semver.outputs.nextStrict }}-${{ inputs.releaseType }}.${releaseNum}" >> "$GITHUB_OUTPUT"
          fi
    outputs:
      bumpVersion: ${{ steps.semver.outputs.bump }}
      releaseVersion: ${{ steps.channel.outputs.version }} # with v prefix
      releaseVersionStrict: ${{ steps.channel.outputs.versionStrict }} # without v prefix
      isPrerelease: ${{ steps.channel.outputs.isPrerelease }}
  create-package-linux:
    if: needs.prepare-release-version.outputs.bumpVersion != 'none'
    needs: [prepare-release-version]
    uses: ./.github/workflows/create-linux-package.yml
    with:
      version: ${{ needs.prepare-release-version.outputs.releaseVersionStrict }}
  create-package-windows:
    if: needs.prepare-release-version.outputs.bumpVersion != 'none'
    needs: [ prepare-release-version ]
    uses: ./.github/workflows/create-windows-package.yml
    with:
      version: ${{ needs.prepare-release-version.outputs.releaseVersionStrict }}
  create-release:
    needs: [prepare-release-version, create-package-linux, create-package-windows]
    if: needs.prepare-release-version.outputs.bumpVersion != 'none'

    runs-on: ubuntu-latest
    steps:
      - name: Create GitHub App token
        id: create-token
        uses: actions/create-github-app-token@v3
        with:
          client-id: ${{ secrets.RELEASE_BOT_CLIENTID }}
          private-key: ${{ secrets.RELEASE_BOT_KEY }}
      - name: Fetch repository
        uses: actions/checkout@v7
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 0
          token: ${{ steps.create-token.outputs.token }}
      - name: Update project version in CMakeLists.txt
        run: |
          version='${{ needs.prepare-release-version.outputs.releaseVersionStrict }}'
          sed -i -E "s/(set\(VSMM_RELEASE_VERSION )\"[^\"]*\"/\1\"${version}\"/" CMakeLists.txt
      - name: Generate CHANGELOG.md
        id: generate-changelog
        uses: orhun/git-cliff-action@v4
        with:
          config: cliff.toml
          args: --tag ${{ needs.prepare-release-version.outputs.releaseVersion }} --unreleased
        env:
          GITHUB_TOKEN: ${{ steps.create-token.outputs.token }}
      - name: Create release commit with tag
        uses: stefanzweifel/git-auto-commit-action@v7
        with:
          commit_user_name: '${{ steps.create-token.outputs.app-slug }}[bot]'
          commit_user_email: '301098493+${{ steps.create-token.outputs.app-slug }}[bot]@users.noreply.github.com'
          commit_author: '${{ steps.create-token.outputs.app-slug }}[bot] <301098493+${{ steps.create-token.outputs.app-slug }}[bot]@users.noreply.github.com>'
          commit_message: 'chore(release): ${{ needs.prepare-release-version.outputs.releaseVersionStrict }}'
          branch: ${{ github.ref_name }}
          commit_options: '--no-verify --signoff'
          file_pattern: 'CMakeLists.txt'
          tag_name: ${{ needs.prepare-release-version.outputs.releaseVersion }}
          tagging_message: 'Release ${{ needs.prepare-release-version.outputs.releaseVersionStrict }}'
          disable_globbing: true
          create_git_tag_only: false
      - name: Download artifacts
        uses: actions/download-artifact@v8
        with:
          path: ${{ github.workspace }}/pkg
          pattern: 'release-packages-*'
          merge-multiple: true
      - name: Create release
        uses: ncipollo/release-action@v1.21.0
        with:
          token: ${{ steps.create-token.outputs.token }}
          name: ${{ needs.prepare-release-version.outputs.releaseVersion }}
          artifacts: 'pkg/*.xz,pkg/*.gz,pkg/*.rpm,pkg/*.deb,pkg/*.exe,pkg/*.zip'
          body: ${{ steps.generate-changelog.outputs.content }}
          tag: ${{ needs.prepare-release-version.outputs.releaseVersion }}
          prerelease: ${{ needs.prepare-release-version.outputs.isPrerelease }}
          makeLatest: ${{ needs.prepare-release-version.outputs.isPrerelease == 'false' }}
          immutableCreate: true
