# cliff.toml — git-cliff configuration for VSModChecker
# Docs: https://git-cliff.org/docs/configuration

[changelog]
# Prepended to every generated changelog
header = """
# Changelog

"""
# The per-release body (Tera template)
body = """
{% if version %}\
## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
## [Unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
### {{ group | striptags | trim | upper_first }}
{% for commit in commits %}
- {% if commit.scope %}**{{ commit.scope }}:** {% endif %}{{ commit.message | upper_first }}\
{% if commit.remote.username %} by @{{ commit.remote.username }}{% endif %}\
{% if commit.remote.pr_number %} ([#{{ commit.remote.pr_number }}](https://github.com/{{ remote.github.owner }}/{{ remote.github.repo }}/pull/{{ commit.remote.pr_number }})){% endif %}\
{% endfor %}
{% endfor %}\n
{%- if github -%}
{% if github.contributors | filter(attribute="is_first_time", value=true) | length != 0 %}
  ## ❤️ New Contributors
{% endif %}\
{% for contributor in github.contributors | filter(attribute="is_first_time", value=true) %}
  * @{{ contributor.username }} made their first contribution
    {%- if contributor.pr_number %} in \
      [#{{ contributor.pr_number }}](https://github.com/{{ remote.github.owner }}/{{ remote.github.repo }}/pull/{{ contributor.pr_number }}) \
    {%- endif %}
{%- endfor -%}
{%- endif %}
"""
trim = true
footer = ""

[git]
conventional_commits = true       # parse messages as Conventional Commits
filter_unconventional = true      # drop commits that don't follow the spec

commit_preprocessors = [
  { pattern = '(?m)\s*\(#\d+\)$', replace = "" },
]

split_commits = false
protect_breaking_commits = true   # never filter out a breaking change
filter_commits = true             # only keep commits matched by a parser below
topo_order = false
sort_commits = "newest"

# Only consider stable version tags and ignore prerelease tags (-alpha/-beta/-rc)
tag_pattern = "v[0-9]+\\.[0-9]+\\.[0-9]+$"

commit_parsers = [
  { message = '^\w+\((ci|build|chore|docs|style)\)', skip = true },
  { message = "^feat",      group = "<!-- 0 -->🚀 Features" },
  { message = "^fix",       group = "<!-- 1 -->🐛 Bug Fixes" },
  { message = "^perf",      group = "<!-- 2 -->⚡ Performance" },
  { message = "^refactor",  group = "<!-- 3 -->♻️ Refactor" },
  { message = "^test",      group = "<!-- 4 -->🧪 Testing" },
  { message = "^deps",      group = "<!-- 5 -->📦 Dependencies" },
  # everything below is dropped from the changelog:
  { message = "^docs",      skip = true },
  { message = "^style",     skip = true },
  { message = "^ci",        skip = true },
  { message = "^build",     skip = true },
  { message = "^chore",     skip = true },
  { message = ".*",         skip = true },   # anything unmatched
]

[remote.github]
owner = "FoxTale-Group"
repo  = "VSMM"
